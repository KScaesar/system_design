# Tiered Storage 設計文件

- **版本** 1.1
- **適用** append-only 的時序資料集
- **讀者** 後端 / 資料工程
- **參考資料** https://netflixtechblog.medium.com/timeseries-tiered-storage-journey-kafka-flink-streams-to-native-cassandra-cold-reads-e59d597c9d60

## 1. 適用條件

依序檢查下列三項。任何一項不成立，則不要導入本設計。

| 檢查項 | 不成立時的作法 |
|---|---|
| 舊資料不可刪除 | 設定 TTL |
| 冷資料需要秒級查詢 | 定期 dump 加還原腳本 |
| Storage 是成本瓶頸 | 若付固定實例費，先縮小實例 |

---

## 2. 術語

| 術語 | 定義 |
|---|---|
| **hot tier** | 線上資料庫。低延遲。高單價 |
| **cold tier** | 物件儲存。高延遲。低單價 |
| **archive unit** | 一段時間區間的資料整體。生命週期的單位 |
| **archive object** | cold tier 上的單一檔案。讀取規劃的單位 |
| **catalog** | 記錄 archive unit 與 archive object 狀態的元資料 |
| **db partition** | 資料庫的實體分區 |
| **source partition** | message queue 的並行單位 |
| **row group** | 欄式檔案內的最小讀取區塊 |

「partition」一詞有三種意思。本文件不單獨使用該詞。

archive unit 與 db partition 不同。一個 archive unit 可對應多個 db partition。archive unit 進入 cold tier 後不對應任何 db partition。

若兩者粒度永遠相同，可以省略 archive unit 這層抽象。

---

## 3. 前提：hot tier 必須支援 O(1) 刪除

分層儲存的省錢動作是刪除 hot tier 副本。若刪除昂貴，則收益不成立。

**不可**逐筆 DELETE。後果如下。

| 資料庫 | 逐筆 DELETE 的後果 |
|---|---|
| PostgreSQL | 產生 dead tuple。磁碟不歸還。回收需 VACUUM FULL |
| Cassandra | 產生 tombstone。每次讀取都掃過 tombstone。讀延遲劇增 |
| MySQL | 空間留在 tablespace。不歸還 OS |

**必須**使用 db partition。

| 資料庫 | 作法 |
|---|---|
| PostgreSQL | declarative range partitioning。以 DROP TABLE 刪除 |
| MySQL | PARTITION BY RANGE。以 DROP PARTITION 刪除 |
| Cassandra | 一個時間窗一張表。以 DROP TABLE 刪除 |

此決定**必須**在上線前完成。事後改造需要完整搬遷資料。

---

## 4. Time Axis

archive unit 封閉後不可再有寫入。

| 選項 | 封閉判斷 | 查詢代價 |
|---|---|---|
| ingestion time | 時間到即封閉。無遲到寫入 | 需多讀相鄰 unit 再過濾 |
| event time | 需等待遲到資料 | 述詞直接對應 unit 邊界 |

**建議**使用 event time 加 late-arrival window。

規則：`now > unit.ts_end + max_lateness` 時才封閉 unit。

`max_lateness` **必須**由實測決定。量測 `ingestion_time - event_time` 的 p99.9。

超過 window 的資料**必須**寫入 quarantine 表。**不可**補寫進已封閉的 unit。補寫會使 catalog 狀態失真，並迫使系統實作跨層去重。

---

## 5. Catalog

### 5.1 分層理由

catalog 有四個職責。

| 職責 | 讀者 | 更新頻率 |
|---|---|---|
| 生命週期狀態機 | archive job | 低 |
| 讀取規劃 | 查詢路徑 | 只讀 |
| 驗證 | verify job | 低 |
| GC | cleanup job | 只讀 |

狀態機的單位是時間窗。讀取規劃的單位是檔案。兩者頻率差異大。**必須**拆成兩張表。

### 5.2 archive_unit

```sql
CREATE TABLE archive_unit (
  unit_id        TEXT PRIMARY KEY,
  namespace      TEXT        NOT NULL,
  ts_start       TIMESTAMPTZ NOT NULL,   -- 半開區間 [ts_start, ts_end)
  ts_end         TIMESTAMPTZ NOT NULL,
  state          TEXT        NOT NULL,
  hot_relation   TEXT,                   -- hot tier 實體表名
  schema_ver     INT         NOT NULL,

  expected_rows  BIGINT,                 -- 自 hot tier 量測
  archived_rows  BIGINT,                 -- 自 cold tier 讀回量測
  expected_ck    NUMERIC,                -- checksum
  archived_ck    NUMERIC,

  sealed_at      TIMESTAMPTZ,
  archived_at    TIMESTAMPTZ,
  purged_at      TIMESTAMPTZ,

  lease_owner    TEXT,
  lease_expires  TIMESTAMPTZ,
  attempt        INT NOT NULL DEFAULT 0,
  last_error     TEXT,
  version        INT NOT NULL DEFAULT 0  -- 樂觀鎖
);

CREATE INDEX ON archive_unit (namespace, ts_start);
CREATE INDEX ON archive_unit (state) WHERE state <> 'COLD';
```

欄位理由：

| 欄位 | 理由 |
|---|---|
| 半開區間 | 閉區間使邊界資料屬於兩個 unit。系統會撈取兩次 |
| `hot_relation` | 命名規則會演進。舊 unit 的表名是歷史事實。不可由 unit_id 推導 |
| `expected_*` 與 `archived_*` | 兩者必須獨立產生。程式自報的數字只證明程式內部一致 |
| `lease_*` | 防止兩個 worker 同時處理同一 unit。使用 lease 而非 lock，因為 worker 會崩潰 |
| `version` | 所有狀態轉移必須是 CAS |
| 部分索引 | 多數 unit 停在終態。部分索引使查詢索引維持數十筆 |

### 5.3 狀態機

```
HOT -> SEALED -> ARCHIVING -> BOTH -> COLD
          ^          |
          +----------+  (驗證失敗)
```

| state | 語意 | 讀取路徑 |
|---|---|---|
| HOT | 接受寫入 | 讀 hot tier |
| SEALED | 停止寫入。未歸檔 | 讀 hot tier |
| ARCHIVING | 歸檔中。cold tier 不可信 | 讀 hot tier |
| BOTH | 兩層皆有。已驗證 | **讀 hot tier** |
| COLD | hot tier 已刪除 | 讀 cold tier |

規則說明：

- SEALED 必須獨立於 HOT。封閉與歸檔之間隔著 late-arrival window。
- BOTH 一律讀 hot tier。此規則使兩層在讀取路徑上永不相交。跨層合併因此退化為串接。
- 驗證失敗退回 SEALED。**不可**新增 FAILED 狀態。失敗資訊記錄於 `attempt` 與 `last_error`。

### 5.4 狀態轉移

```sql
UPDATE archive_unit
   SET state = 'ARCHIVING',
       lease_owner = :worker,
       lease_expires = now() + interval '30 minutes',
       attempt = attempt + 1,
       version = version + 1
 WHERE unit_id = :unit_id
   AND version = :expected_version
   AND state = 'SEALED'
   AND (lease_expires IS NULL OR lease_expires < now());
```

檢查 affected rows。若為 0，則放棄此 unit。

`AND state = 'SEALED'` **不可**省略。重試時 version 可能恰好吻合。狀態守衛使轉移不可倒退。

### 5.5 archive_object

```sql
CREATE TABLE archive_object (
  object_id     BIGSERIAL PRIMARY KEY,
  unit_id       TEXT        NOT NULL REFERENCES archive_unit(unit_id),
  object_key    TEXT        NOT NULL,
  content_hash  TEXT        NOT NULL,
  ts_min        TIMESTAMPTZ NOT NULL,     -- 檔內實際範圍
  ts_max        TIMESTAMPTZ NOT NULL,
  row_count     BIGINT      NOT NULL,
  byte_size     BIGINT      NOT NULL,

  src_partition INT,                      -- streaming 專用
  offset_start  BIGINT,
  offset_end    BIGINT,                   -- 半開

  created_at    TIMESTAMPTZ NOT NULL DEFAULT now()
);

CREATE UNIQUE INDEX ON archive_object (object_key);
CREATE INDEX ON archive_object (unit_id, ts_min, ts_max);
CREATE UNIQUE INDEX ON archive_object (src_partition, offset_start)
  WHERE src_partition IS NOT NULL;
```

欄位理由：

| 欄位 | 理由 |
|---|---|
| `ts_min` / `ts_max` | 讓 reader 在發出請求前排除檔案。此為冷讀延遲的主因 |
| `content_hash` | 偵測物件已被覆寫。使 reader 的快取失效 |
| `object_key` 唯一索引 | 使重跑冪等。`ON CONFLICT DO UPDATE` 即為 commit |
| `(src_partition, offset_start)` 唯一索引 | 唯一能主動偵測資料遺失的機制 |

`ts_min` 與 `ts_max` **必須**是實際值。streaming 模式按攝取順序切檔。檔案的 event time 範圍不等於 unit 範圍。

### 5.6 不可放入 catalog 的資料

**不可**為高基數欄位記錄 min/max。

檔案按時間切分時，每個檔案涵蓋幾乎所有實體。統計範圍等於全域範圍。裁剪率為零。

高基數維度的裁剪**必須**發生在 row group 層級。見第 7 節。

若使用 hash bucket 分流，可以記錄 `bucket_id`。此為分桶識別，非統計範圍。

### 5.7 Catalog 位置

**必須**使用既有的 OLTP 資料庫。**不可**存放於物件儲存。

| 需求 | 物件儲存的問題 |
|---|---|
| CAS 與 lease | 無成熟的條件更新語意 |
| 範圍查詢 | 只能全讀或猜測 key |

catalog 很小。streaming 模式三年約 380 萬列，僅數百 MB。

### 5.8 Catalog 容量管理

streaming 模式需做兩件事。

1. `archive_object` 依 `created_at` 分區，以便清理過期記錄。
2. unit 進入終態後，將物件列表摺疊為單一 JSON 欄位，並刪除明細列。終態 unit 的列表不再變動。

---

## 6. Archive 路徑

### 6.1 模式選擇

| 模式 | 適用 |
|---|---|
| batch | 延遲需求大於 1 小時 |
| streaming | 延遲需求小於 1 小時。或來源本身是 message queue。或來源資料庫無法承受大範圍掃描 |

**建議**使用 batch。batch 的資料源是資料庫本身。streaming 是 dual-write，需自行處理兩邊不一致。

### 6.2 Batch 流程

1. 掃描 `state='SEALED'` 且 lease 可取得的 unit。
2. 以 CAS 取得 lease。轉為 `ARCHIVING`。
3. 自 hot tier 量測 `expected_rows` 與 `expected_ck`。寫回 catalog。
4. 讀取。排序。編碼。寫入 cold tier。使用確定性 object key。
5. 自 cold tier 讀回。量測 `archived_rows` 與 `archived_ck`。
6. 比對。不符則退回 `SEALED`。相符則轉為 `BOTH`。
7. 於**另一次執行**中，刪除超過冷卻期的 `BOTH` unit。轉為 `COLD`。

### 6.3 Streaming 約束

#### 6.3.1 Offset commit 順序

**警告**：順序錯誤會造成靜默的資料遺失。測試環境無法重現。

**必須**依下列順序執行。

1. 寫入 cold tier。
2. 寫入 catalog。
3. Commit offset。

任何一點崩潰只會造成重複，不會造成遺失。

**必須**關閉 auto commit。

#### 6.3.2 確定性 object key

上述順序保證 at-least-once。重複資料會使查詢出現重複列。

**必須**由上游位置決定 object key。**不可**含時間戳或隨機值。

```
{prefix}/{namespace}/dt={date}/hr={hour}/p={partition}/off={start}-{end}.parquet
```

重跑時 key 相同，內容相同。直接覆寫即可。

切檔邊界**必須**由 offset 決定。將實際涵蓋的 offset 區間寫入 key。

#### 6.3.3 Flush 觸發條件

**必須**同時設定三個條件。任一滿足即 flush。

```
flush = bytes >= size_limit
     or rows  >= row_limit
     or age   >  time_limit
```

只依時間會在低流量時產生小檔。只依大小會使資料無限期滯留於記憶體。

**必須**記錄實際觸發的條件。此為調參的唯一依據。時間觸發佔比超過 80% 表示檔案過小。

#### 6.3.4 分流度

```
檔案數 = 分流度 × flush 次數
```

分流度是小檔問題的乘數。

- 路徑層級**只放**時間維度。
- 高基數欄位放進檔案內容並排序。
- 若需按 key 分流，使用 hash bucket。桶數等於消費者並行度。

#### 6.3.5 Buffer

一個 worker **只維護**一個開啟中的 buffer。多個 buffer 使記憶體用量成倍增加。

**必須**使用有界佇列。滿載時阻塞消費迴圈以形成 backpressure。

**不可**使用無界佇列加上丟棄策略。

注意 message queue 的 poll timeout。**建議**將處理置於獨立執行緒，使主迴圈維持 poll 節奏。

---

## 7. Cold Tier 檔案格式

**必須**使用欄式格式，例如 Parquet 或 ORC。價值在於可以只讀檔案的一部分。

### 7.1 排序

row group 的 min/max 統計只在資料依查詢述詞排序時有鑑別度。

若欄位隨機散佈，則每個 row group 的統計範圍等於全域範圍。裁剪率為零。

排序欄位**必須**對應冷查詢的述詞，非主鍵。典型為 `(entity_id, event_time)`。

### 7.2 Row group 大小

row group 是最小讀取單位。

| 過大 | 過小 |
|---|---|
| 點查詢為一筆資料讀整個 row group | footer 中繼資料膨脹。每次開檔都完整讀取 footer |

無通用最佳值。

| 冷查詢型態 | 建議 |
|---|---|
| 點查詢為主 | 數 MB。明顯小於格式建議值 |
| 範圍掃描為主 | 使用預設值 |

**必須**先確認查詢型態再設定。

### 7.3 壓縮

cold tier 在意儲存費與傳輸量，不在意解壓延遲。

**建議**使用 ZSTD 而非 Snappy。典型差距 30% 至 40%。壓縮等級取中等即可。

### 7.4 檔案大小

每次讀檔至少需兩次請求：一次取 footer，一次取 row group。此開銷在小檔上佔比極高。

**建議**單檔 128 MB 至 1 GB。

### 7.5 記憶體

欄式格式需累積整個 row group 才能編碼。footer 位於檔案尾端。因此**無法**串流式輸出。

記憶體用量約為 `並行度 × 檔案大小 × 1.5 至 2`。

若超出可用記憶體，則改寫本地暫存檔再上傳。注意容器磁碟配額。

---

## 8. 驗證與刪除

### 8.1 驗證

**警告**：刪除 hot tier 是唯一不可逆的操作。

**必須**自 cold tier 讀回資料並比對。**不可**只信任「程式未報錯」。

最危險的失敗是靜默的部分成功。下列情況不會拋出例外。

- 連線中途斷開但程式認為完成。
- 游標邏輯遺漏邊界記錄。
- 時區處理錯誤使資料落入錯誤的 unit。

### 8.2 Checksum 選擇

| checksum | 偵測 | 無法偵測 |
|---|---|---|
| `count(*)` | 筆數遺失 | 欄位錯位 |
| `sum(數值欄)` | 數值損壞 | 非數值欄位 |
| `sum(hash(pk))` | 記錄身分錯誤 | 非鍵欄位 |

**必須**至少使用 `count(*)` 加一個內容敏感的聚合。

### 8.3 冷卻期

**不可**將 archive 與 delete 置於同一次執行。

驗證邏輯本身也可能有缺陷。耦合時，缺陷會在被發現前消耗完原始資料。

**建議**冷卻期至少 7 天。重複儲存的成本遠低於資料遺失的期望損失。

### 8.4 Commit point

物件儲存無 rename，也無跨物件交易。

**必須**滿足兩項條件。

1. 使用確定性 object key。
2. Catalog 的狀態更新是唯一的 commit point。

失敗狀態一律是「cold tier 有孤兒物件但 catalog 未 commit」。reader 只信任 catalog。孤兒物件由 GC job 清除。

---

## 9. Read 路徑

### 9.1 查詢規劃

hot tier 與 cold tier 在時間軸上不重疊。規劃因此很單純。

```sql
-- 1. 找相關 unit
SELECT unit_id, state, hot_relation
FROM archive_unit
WHERE namespace = :ns AND ts_start < :t2 AND ts_end > :t1
ORDER BY ts_start;

-- 2. 對終態 unit 取得物件
SELECT object_key, ts_min, ts_max, row_count
FROM archive_object
WHERE unit_id = ANY(:cold_unit_ids)
  AND ts_min < :t2 AND ts_max >= :t1
ORDER BY ts_min;
```

結果是有序的執行計畫。按時間序串接即可。

### 9.2 Catalog 快取

上述查詢位於熱路徑。

| 表 | 快取策略 |
|---|---|
| `archive_unit` | 行程內全量快取。TTL 30 秒 |
| `archive_object` | 以 unit_id 為鍵的 LRU。終態 unit 可無限期快取 |

**不需要**外部快取服務。

### 9.3 分頁

**必須**使用 `(event_time, record_id)` 複合游標。

| 方案 | 問題 |
|---|---|
| offset | 跨資料源時算不出。資料源內部亦為 O(n) |
| 資料源內部游標 | 跨越層邊界時無法接續 |

`record_id` 處理同一時間戳多筆的情況。只用時間戳會在邊界重複或遺漏。

---

## 10. 基礎設施

### 10.1 網路路徑

**警告**：NAT gateway 的每 GB 費用可能高於物件儲存的儲存單價。

**必須**使用 VPC gateway endpoint 或等效的私有端點。此為零成本的設定變更。

上線前**必須**實測流量路徑。

### 10.2 Storage class

| class | 適用 |
|---|---|
| standard | 起點 |
| infrequent access | 有一年實際存取分佈後 |
| archive | 取回延遲與秒級查詢衝突。通常不適用 |

若不確定，可以使用 intelligent tiering。

### 10.3 Multipart upload

**建議**大檔使用 multipart upload。理由是失敗成本，非大小上限。

**警告**：未完成的 multipart upload 佔用空間並持續計費，但在一般物件列表中看不到。

**必須**設定 lifecycle rule，自動清除超過 N 天的未完成 upload。

### 10.4 Prefix 速率

請求速率上限按 prefix 計算。

對多數自建規模，此限制不會觸發。觸及上限意味著每秒數千個檔案。

**必須**開啟 SDK 的 retry，並使用 exponential backoff 加 jitter。

---

## 11. 監控

### 11.1 指標

失敗多為慢性，不觸發錯誤告警。

| 指標 | 偵測 | 告警條件 |
|---|---|---|
| consumer lag | 是否跟得上 | 持續成長超過 N 分鐘 |
| 檔案大小分佈 | 小檔問題 | p50 小於目標值一半 |
| flush 觸發原因分佈 | 調參依據 | 時間觸發超過 80% |
| unit 狀態滯留時長 | 卡住的 job | ARCHIVING 超過 lease 期限 |
| `attempt` | 反覆失敗 | 大於 3 |

### 11.2 連續性檢查

此為唯一能主動偵測資料遺失的機制。**必須**定期執行。

```sql
SELECT src_partition, prev_end, offset_start
FROM (
  SELECT src_partition, offset_start,
         lag(offset_end) OVER (
           PARTITION BY src_partition ORDER BY offset_start
         ) AS prev_end
  FROM archive_object
  WHERE src_partition IS NOT NULL
) t
WHERE prev_end IS NOT NULL AND prev_end <> offset_start;
```

結果非空即告警。

batch 模式則檢查 unit 的時間區間是否連續。

### 11.3 重放能力

| 模式 | 前提 |
|---|---|
| batch | 冷卻期內可直接重跑 |
| streaming | message queue 保存期需長於發現問題所需時間。建議 7 天 |

確定性 key 使重放時直接覆寫。**不需要**先刪除舊物件。

---

## 12. Schema 演進

cold tier 檔案是不可變的歷史快照。舊檔案不含新增欄位。

**建議**由 catalog 記錄 `schema_ver`。reader 依版本決定欄位對應，並為缺失欄位補預設值。

catalog 記錄版本使 reader 在查詢規劃階段即可決定讀取邏輯。若依賴檔案自我描述，則需先開檔。

| schema 變動頻率 | 建議 |
|---|---|
| 一年一兩次 | 手工版本對應 |
| 頻繁。或需要 time travel | 評估 table format，例如 Iceberg 或 Delta Lake |

table format 引入 catalog 服務、中繼資料維護、compaction 排程。當你發現自己在手工重建這些功能時，才是遷移時機。

---

## 13. 決策速查

| 決策點 | 選擇 | 判準 |
|---|---|---|
| hot tier 刪除 | db partition drop | 必須。無替代方案 |
| time axis | event time 加 window | 讀取簡單優先 |
| archive 模式 | batch | 延遲需求大於 1 小時 |
| 生命週期單位 | archive unit | 粒度可能不同時才需要 |
| catalog 位置 | OLTP 資料庫 | 需要 CAS 與範圍查詢 |
| 跨層重疊 | 禁止 | BOTH 讀 hot tier |
| row group 大小 | 依查詢型態 | 需量測 |
| 壓縮 | ZSTD | 高壓縮率優先 |
| 刪除時機 | 冷卻期後 | 至少 7 天 |
| schema 管理 | `schema_ver` 欄位 | 變動頻繁時才用 table format |

---

## 14. 導入順序

### 14.1 必要項目

依失敗後果排序。

| 順序 | 項目 | 未做的後果 |
|---|---|---|
| 1 | hot tier 分區化 | 省不到錢 |
| 2 | catalog | 靜默的查詢缺漏 |
| 3 | 驗證加冷卻期 | 永久資料遺失 |
| 4 | 私有網路端點 | 成本反而增加 |

### 14.2 優化項目

可先做粗糙版本。

| 項目 | 何時做 |
|---|---|
| 排序與 row group 大小 | 有真實查詢分佈後 |
| catalog 快取 | 量測到讀取延遲問題後 |
| catalog compaction | 資料量到達後 |
| schema 版本管理 | 第一次 schema 變更時 |

### 14.3 第一次迭代範圍

```
hot tier 分區化
  -> catalog 兩張表加五個 state
  -> batch 匯出。單執行緒
  -> 讀回驗證
  -> 冷卻期 7 天後刪除
  -> reader：catalog 查詢加欄式格式讀取
  -> 連續性健康檢查
```

**不含** streaming。**不含**快取。**不含** hash bucket。

跑滿一個保存週期。確認節省額度符合預期。再依實際指標決定下一步。