# State / Status / Condition / Guard / Result 命名規範

## TL;DR


- `state` 管「下一步能做什麼」
- `result` 管「這次做完的定案是什麼」
- `condition` 管「有哪些可以並存的觀測事實」
- `status` 管「代表當下、會被覆寫的彙總讀數」
- `guard` 是轉移發生當下才求值的判斷式，可能讀取 condition 當輸入，但不是獨立存在的欄位

判準不是「這個詞聽起來像不像結果」，而是問兩件事：

1. **會不會決定下一步合法動作？** → 會，就是 `state`
2. **值會不會隨時間被反覆覆寫，代表「現在」這個瞬間？**
   - 會覆寫、代表當下 → `status`
   - 寫死封存、代表某一次已結束執行的定案 → `result`
3. **是不是一組彼此獨立、可以同時成立的觀測事實？** → `condition`

四個詞對應四個完全不重疊的結構性職責，不是四種同義詞挑一個順眼的。

---

## 判準表

| 詞 | 結構性定義 | 判準問句 | 轉移規則 | 互斥性 | 可變性 |
|---|---|---|---|---|---|
| **state** | 一個枚舉，決定實體**下一步能做什麼**，同一層只有一個值 | 這個值變了，合法動作集合會不會跟著變？ | 有，且明確定義 | 是 | 依轉移規則單向推進 |
| **result** | 綁定**一次有界執行**，寫死封存的定案 | 這件事做完了嗎？做完的結果是什麼？ | 只有一次：null → 定值，不可逆 | 是（僅在終態時存在） | 寫一次就不再變；重試會產生新的一筆，不覆寫舊的 |
| **condition** | 一組**獨立、可並存**的觀測事實 | 這幾件事是不是可以同時成立，且各自獨立變化？ | 每個 condition 自己可能 flip，但不是「下一步許可」的依據 | 否，可並存 | 各自獨立變化 |
| **status** | 一個**綁定持續存在實體**、隨時間被反覆覆寫的「目前讀數」，可能是彙總算出來的，也可能直接就是原始讀數 | 這個值代表的是不是「現在」這一瞬間，會不會被下一次觀測覆寫掉？ | 沒有自己獨立的轉移規則 | 依情境而定 | 反覆覆寫，代表當下 |

---

## 決策流程

```
這個值會不會決定下一步合法動作？
├─ 會 → 只有一個值（互斥）             → state
│        會不會有多個獨立事實同時成立？
│        └─ 是 → 每個都用一個獨立的     → condition
│
├─ 不會，它只在「這次執行結束」時才有意義
│        → 執行前為 null，執行完寫死一次 → result
│
└─ 不會，它代表「現在」這一瞬間，
         會被下一次觀測 / 下一次重試覆寫  → status
```

一個欄位如果同時符合「有轉移規則」又「不互斥」——代表把兩件事混在一起了，先拆開再命名。

---

## 哪個字比較像結果

`result` 和 `status` 表面上都在講「一種結果」，容易混淆，但區分點不是語意像不像，而是**可變性 + 綁定範圍**：

- **result**：綁定一次有界執行（一次 activity 呼叫、一次 request），寫死封存，是原始事實（不可重算的外部回傳值）。重試要保留歷史，就必須讓每次執行都是獨立一筆（PK 要包含 attempt），不能覆寫。
- **status**：綁定一個持續存在、活得比任何一次執行都長的實體，代表「目前」讀數，會被反覆覆寫，可能是彙總多個底層 state/condition/result 算出來的顯示值。

**HTTP status code 是反例但不是推翻判準的證據**：它其實符合 `result` 的行為（綁定一次 request、寫死不變），只是命名早於這套判準十幾年（HTTP/1.0, 1996），是歷史遺留用詞，不代表新設計該遷就它。

---

## condition ≠ guard（前置不變量）

容易誤把 condition 當成「滿足了才能觸發轉移的前置檢查」，但那其實是另一個獨立概念 **guard**，condition 只是 guard *可能*讀取的其中一種輸入，不是 guard 本身。

| | condition | guard |
|---|---|---|
| 存在方式 | **持久化**的獨立事實，平常就存在資料庫/物件裡 | 通常是**當下算出來**的布林運算式，掛在轉移箭頭上，不一定要存 |
| 生命週期 | 一直存在、被反覆更新，跟轉移事件無關 | 只在「有人嘗試觸發這次轉移」的那一刻被求值 |
| 用途 | 給外部系統/人讀，診斷用；**不一定**被任何轉移引用 | 只服務於「這次轉移准不准」這一件事 |
| 若刪除 | 系統照常運作，只是少了一份觀測資訊 | 轉移規則本身消失，行為改變 |

反例證明兩者不能劃等號：Kubernetes 的 `Ready` condition 從來不會觸發 `Pod.phase` 的轉移——`phase` 是另一個 controller 算的，完全不讀 `Ready`。`Ready` 存在的唯一目的是給 kubelet、Service endpoint 選擇器讀，純觀測用，不是任何轉移的守衛。

拿訂單場景對照：

```
conditions（持久化、獨立、隨時可能被讀）：
  PaymentVerified   = true
  FraudCheckPassed  = false
  InventoryReserved = true

guard（轉移發生瞬間才求值，不是欄位）：
  CREATED → PAID 這條轉移的條件是：
    PaymentVerified == true AND FraudCheckPassed == true
```

`FraudCheckPassed=false` 本身是 condition；「這條轉移需要 `FraudCheckPassed=true`」這句規則才是 guard。guard 會讀 condition 當輸入，但就算業務決定拿掉這條 guard 規則，`FraudCheckPassed` 這個 condition 欄位依然可以繼續存在、繼續被記錄——兩者是不同層次的東西，不要合併。

一句話：**condition 的定義是「獨立、可並存、持久化的觀測事實」，跟它有沒有被用來守某個轉移無關；guard 是「轉移發生當下才求值的判斷式」，可能會讀 condition 當輸入。**
